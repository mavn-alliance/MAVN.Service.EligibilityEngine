apiVersion: v1
kind: Service
metadata:
  labels:
    app: eligibilityengine
    environment: Development
  name: eligibilityengine
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: eligibilityengine
  sessionAffinity: None
  type: ClusterIP
